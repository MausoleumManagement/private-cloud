apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cnpg-operator
  #this finaliser causes cascading deletion (delete children)
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
  destination:
    namespace: cnpg-system
    server: "https://kubernetes.default.svc"
  project: unterbau
  source:
    path: manifests/cnpg-operator
    repoURL: "https://github.com/MausoleumManagement/cnpg.git"
    targetRevision: master
