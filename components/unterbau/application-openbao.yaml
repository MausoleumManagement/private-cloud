apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openbao
  #this finaliser causes cascading deletion (delete children)
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  syncPolicy:
    automated:
      enabled: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
  destination:
    namespace: openbao
    server: "https://kubernetes.default.svc"
  project: unterbau
  source:
    path: manifests/openbao
    repoURL: "https://github.com/MausoleumManagement/openbao.git"
    targetRevision: master
